[build]
  command = "bundle exec jekyll build"
  publish = "_site"

[[redirects]]
  from = "/*"
  to = "/:splat/"
  status = 301
  force = true

[slug]
  trailing_slash = "always"  # Options: "always", "never", "ignore"
